---
import { getCollection } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';
import ReviewCard from '../../components/ReviewCard.astro';

const entries = (await getCollection('manga'))
  .sort((a, b) => b.data.date.localeCompare(a.data.date));

const labels = [...new Set(entries.map(e => e.data.label).filter(Boolean))].sort() as string[];
---

<BaseLayout title="漫画" activeNav="manga">
  <div class="page-header">
    <p class="eyebrow">MANGA</p>
    <h1>漫<span>画</span></h1>
    <p class="desc">読んだ漫画の個人レビュー。</p>
  </div>

  <div class="controls">
    <div class="filter-group">
      <label for="f-label">レーベル</label>
      <select id="f-label">
        <option value="">すべて</option>
        {labels.map(l => <option value={l}>{l}</option>)}
      </select>
    </div>
    <div class="filter-group">
      <label for="f-score">最低スコア</label>
      <input type="range" id="f-score" min="1" max="5" step="0.5" value="1">
      <span class="score-display" id="score-val">1.0</span>
    </div>
    <div class="spacer"></div>
    <span class="count-badge"><strong id="count">{entries.length}</strong> 件</span>
  </div>

  <div class="card-grid">
    {entries.map(e => (
      <div class="filter-item"
           data-label={e.data.label ?? ''}
           data-score={e.data.score}>
        <ReviewCard category="manga" slug={e.slug} {...e.data} />
      </div>
    ))}
    <div class="empty-state" id="empty-state" style="display:none">
      <div class="icon">🔍</div>
      <p>条件に合う漫画が見つかりませんでした。</p>
    </div>
  </div>
</BaseLayout>

<style>
  .filter-item { display: contents; }
  .filter-item.hidden { display: none; }
</style>

<script>
  const scoreInput = document.getElementById('f-score') as HTMLInputElement;
  const scoreVal   = document.getElementById('score-val')!;
  const countEl    = document.getElementById('count')!;
  const emptyEl    = document.getElementById('empty-state')!;
  const items      = [...document.querySelectorAll<HTMLElement>('.filter-item')];

  scoreInput.addEventListener('input', () => {
    scoreVal.textContent = parseFloat(scoreInput.value).toFixed(1);
    applyFilters();
  });
  document.getElementById('f-label')!.addEventListener('change', applyFilters);

  function applyFilters() {
    const label    = (document.getElementById('f-label') as HTMLSelectElement).value;
    const minScore = parseFloat(scoreInput.value);
    let visible = 0;
    items.forEach(item => {
      const show = (!label || item.dataset.label === label)
                && parseFloat(item.dataset.score!) >= minScore;
      item.classList.toggle('hidden', !show);
      if (show) visible++;
    });
    countEl.textContent   = String(visible);
    emptyEl.style.display = visible === 0 ? 'block' : 'none';
  }
</script>
